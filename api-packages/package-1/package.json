{
  "name": "@starting-again/package-1",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest  --config ../../_config-vite/vite.non-web.config.js --coverage",
    "lint:es": "eslint **/*.js --ignore-path ../../.eslintignore",
    "prettier": "prettier --config ../../.prettierrc.yaml --ignore-path ../../.prettierignore --check .",
    "prettier:fix": "npm run prettier -- --write"
  }
}
