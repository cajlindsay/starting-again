{
  "name": "package-1",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest  --config ../../vite.non-web.config.js --coverage",
    "lint:es": "eslint **/*.js",
    "prettier": "prettier --config ../../.prettierrc.js --ignore-path ../../.prettierignore --check .",
    "prettier:fix": "npm run prettier -- --write"
  }
}
