apiVersion: networking.gke.io/v1
kind: ManagedCertificate
metadata:
  name: managed-cert
  namespace: starting-again
spec:
  domains:
    - api.txtack.net
    - web-1.txtack.net
    - web-2.txtack.net